/* to find the are of triangle. */
#include<stdio.h>
void main()
{
    float a,b;
    printf("Enter Base of Triangle:");
    scanf("%f",&a);
    printf("Enter Height of Triangle:");
    scanf("%f",&b);
    printf("Area of the triangle is %.2f",0.5*a*b);
}